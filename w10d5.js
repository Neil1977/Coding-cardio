//NO PHONE, NO EMAILS, NO COPY AND PASTING!!!
​
//write a for loop that counts from 0 to 10 in the console
for (let i = 0; i < 11; i++) {
console.log(i);
}

// write a for loop that counts from 0 to 20 in the console.
for (let i = 0; i < 21; i++) {
    console.log(i);
​}
// write a for loop that counts from 10 to 0 in the console.
for (let i = 11; i > 0; i--) {
    console.log(i);
​}
// write a for loop that counts from 20 to 0 in the console.
for (let i = 21; i > 0; i++) {
    console.log(i);
​}
//write a for loop that counts from 0 to 10 by 2's in the console (0,2,4,6,8,10)
for (let i = 0; i < 11; i+=2) {
    console.log(i);
​}
//write a for loop that counts from 10 to 0 in the console.
for (let i = 11; i > 10; i--) {
    console.log(i);
​}
//=============================================================================================================
//=============================================================================================================
//=============================================================================================================
​
// write a function that adds two numbers.  call the function in a console log.
​
function adds(x, y) {
return x + y;
}
console.log(adds(2, 4));
​
//write a function that doubles a number

​function doubler(x) {
    return x * 2;
    }
    console.log(doubler(2));
// write a function that adds three numbers

function addsThree(x, y, z) {
    return x + y + z;
    }
    console.log(addsThree(2, 4, 6));
    ​
//write a function that multiplies a number by 10

​function multiplier(x, y, z) {
    return (x * y) * z;
    }
    console.log(multiplier(2, 4, 6));

//write a function that adds an exclaimation mark to a string
function shocked(a) {
    let a = "SHOCKED" 
    return a + "!";
    }
    console.log(shocked);

//write a function that joins two strings together.
function joiner(a, b) {
    return (str.concat('', ''));
}
console.log(joiner(a,b))

//NO PHONE, NO EMAILS, NO COPY AND PASTING!!!

//write a for loop that counts from 0 to 10 in the console
​for (let i = 0; i < 11; i++) {
    console.log(i);
    }

//write a for loop that counts from 10 to 0 in the console
​
for (let i = 11; i > 11; i--) {
console.log(i);
}
​
//=============================================================================================================
//=============================================================================================================
//=============================================================================================================
​
// write a function that adds two numbers.  call the function in a console log.
​
function adds(x, y) {
return x + y;
}
console.log(adds(2, 4));
​
//write a function that triples a number
function tripler(x) {
return x * 3;
}
console.log (tripler(2));

// write a function that adds three numbers
function addThree(x, y, z) {
    return x + y + z;
}
console.log (addThree(2, 4, 6));
​
//write a function that joins two strings together.
function plusStrings()
return (str.concat('', ''));
console.log(plusStrings("dog", "cat"));
​
//write a function that accepts an array and logs its values out.  (combining function and for-loop)
function arrayLogger(numbers) {
    let numbers = [1, 2, 3, 4];
    for (let i = 0; i < numbers.length; i++)
    console.log(arrayLogger[i]);
}

